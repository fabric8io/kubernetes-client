{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Terminal debugowania JavaScript",
            "request": "launch",
            "cwd": "${workspaceFolder}"
        }
    ]
}