#
# Copyright (C) 2015 Red Hat, Inc.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#         http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#

name: Revapi Compatibility Report

on:
  workflow_dispatch:
    inputs:
      old_version:
        description: 'Published version to compare against (e.g., 7.0.0). Leave empty to compare against the latest published release.'
        required: false
        default: ""
        type: string
      new_version:
        description: 'Published version to compare with (e.g., 7.1.0)'
        required: false
        default: ""
        type: string
      java_version:
        description: 'Java version to use'
        required: false
        default: '17'
        type: choice
        options:
          - '17'
          - '21'

env:
  MAVEN_ARGS: -B -C -V -ntp -Dhttp.keepAlive=false -e

permissions:
  contents: read

jobs:
  revapi-report:
    name: Generate Revapi Report
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set open files (threads) limit
        run: |
          # $$ is the current shell PID (standard shell syntax, not a GitHub Actions expression)
          sudo prlimit --pid $$ --nofile=500000:500000

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          java-version: ${{ inputs.java_version }}
          distribution: 'temurin'
          cache: 'maven'

      - name: Setup JBang
        uses: jbangdev/setup-jbang@main

      - name: Generate Revapi Reports
        env:
          REVAPI_OLD_VERSION: ${{ inputs.old_version }}
          REVAPI_NEW_VERSION: ${{ inputs.new_version }}
        run: |
          if [ -z "$REVAPI_NEW_VERSION" ]; then
            echo "Comparing current code against published version ${REVAPI_OLD_VERSION:-latest}"
            make revapi-report OLD_ARTIFACT_VERSION="$REVAPI_OLD_VERSION"
          else
            echo "Comparing two published versions: $REVAPI_OLD_VERSION -> $REVAPI_NEW_VERSION"
            make revapi-compare-jars \
              OLD_ARTIFACT_VERSION="$REVAPI_OLD_VERSION" \
              NEW_ARTIFACT_VERSION="$REVAPI_NEW_VERSION"
          fi

      - name: Set artifact name
        id: artifact-name
        env:
          OLD_VER: ${{ inputs.old_version }}
          NEW_VER: ${{ inputs.new_version }}
        run: |
          if [ -z "$NEW_VER" ]; then
            echo "suffix=vs-${OLD_VER:-latest}" >> $GITHUB_OUTPUT
          else
            echo "suffix=${OLD_VER}-to-${NEW_VER}" >> $GITHUB_OUTPUT
          fi

      - name: Compress Reports
        run: tar -czf revapi-reports.tar.gz -C target/staging .

      - name: Upload Reports
        uses: actions/upload-artifact@v6
        with:
          name: revapi-reports-${{ steps.artifact-name.outputs.suffix }}
          path: revapi-reports.tar.gz
          retention-days: 30

      - name: Summary
        env:
          OLD_VER: ${{ inputs.old_version }}
          NEW_VER: ${{ inputs.new_version }}
          SUFFIX: ${{ steps.artifact-name.outputs.suffix }}
        run: |
          echo "## Revapi Compatibility Report Generated" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ -z "$NEW_VER" ]; then
            echo "**Comparison:** Current code vs. published version \`${OLD_VER:-latest}\`" >> $GITHUB_STEP_SUMMARY
          else
            echo "**Comparison:** Published version \`${OLD_VER}\` vs. \`${NEW_VER}\`" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Download the \`revapi-reports-${SUFFIX}\` artifact and open \`revapi-index.html\` in your browser to view the reports." >> $GITHUB_STEP_SUMMARY
